# UIED-Tools 项目协作规则

## 沟通

- 面向用户的叙述默认使用简体中文；代码、命令和技术标识保持英文。
- 先给影响与结论，再给行动、待决策和必要证据；没有对应内容就省略。
- 使用简洁、连贯的段落；只有确实适合并列比较或按步骤执行时才使用列表。
- 使用具体、简单的词，避免无意义术语、套话、重复总结和未经请求的对比。
- 技术细节只保留对理解结论、判断风险或复现结果有帮助的部分。

## 指令优先级

- 遵守系统、平台和安全约束。
- 用户当前明确指令优先于 Skill、历史记忆和默认偏好。
- 本文件只补充 UIED-Tools 项目规则，不能覆盖系统、平台、安全和运行环境约束。

## 执行方式

- 用户表示要开始新工作或修复现有问题时，持续推进，直到用户的目标完成。
- 开始修改前先查看 `git status` 和相关 `git diff`，确认已有用户改动；禁止使用 `reset`、强制 checkout 或其他方式覆盖未授权改动。
- 主站继续使用 Vue 3。React 只用于已有 React 生态或独立工具，并通过路由级宿主接入，不在同一业务组件树中混用。
- 设计改动遵循 `docs/frontend-design-system-v3.md`。用户明确要求保留现有设计时，不主动重做页面视觉。
- API 优先通过前端服务层和 Go 后端网关接入，不在页面组件中新增不稳定的第三方 API 直连。
- 版本升级、发布检查和 Git 提交单独处理，不与普通功能开发混在一起；除非用户明确要求，不主动升级版本号或提交 Git。

## 项目环境

- 项目路径：`/Users/tangxiaoda/Desktop/网站备份/tools-web-master 2/tools-web`
- 系统：Mac。
- 前端开发服务：`http://localhost:5179`。
- Go 后端服务：`http://localhost:8003`。
- 后台管理服务：`http://localhost:5180`。
- 当前产品规划版本：3.0.3。`package.json`、发布文件和页面版本显示需要保持一致；版本变更必须单独确认范围。

## 代码与页面规范

- 新增函数必须添加函数级中文注释，说明作用、参数和返回值（适用时）。
- 页面文件保留以下版权信息：

  ```ts
  /**
   * @copyright Tomda (https://www.tomda.top)
   * @copyright UIED技术团队 (https://fsuied.com)
   * @author UIED技术团队
   * @createDate
   */
  ```

- 新增界面文案默认使用简体中文，除非工具功能或品牌名称确实需要英文。
- 交互应支持键盘操作、清晰焦点状态、合理的加载和错误反馈，并避免无意义的动画和装饰。

## 测试与验证

- 不要为可逆、影响小、只是复述实现的改动编写测试。
- 跑与本次改动相称的测试，并完成必要检查。常用检查包括：
  - `npx vue-tsc --noEmit`
  - `npm run test:unit -- --run`
  - `npm run build:check`
  - `git diff --check`
- 涉及页面交互时使用实际浏览器回归，至少检查 320/375/768/1440px 中与改动相关的尺寸。
- 涉及服务联动时确认前端、Go 后端和后台管理服务的实际接口状态。
- 验证通过后，只有出现新改动、新失败或尚未解决的疑点时，才扩大或重复测试。
- 收尾删除本次产生且之后不再使用的临时文件。

## 工具与并行

- 搜索文件或文本优先使用 `rg`、`rg --files`；独立的读取和查询尽量批量执行。
- 本地网页控制台无 CLI/API 时使用已登录的 Chrome 或 Codex 浏览器进行验证。
- 只有存在真正独立的工作流，且委派能节省时间或提升质量时才使用子 Agent。
- 共享状态、连续决策和简单任务由当前 Agent 直接完成；委派任务必须有明确输入、输出和完成判据，最终结论由主 Agent 汇总并验证。

## 规则来源

- 本文件是 UIED-Tools 项目级规则的 canonical 来源。
- `CLAUDE.md` 只作为兼容入口，不复制本文件正文。
- 项目事实、生产状态、历史决策和对外契约以本文件及其指定的脚本、探针、决策记录和合同文件为准。
